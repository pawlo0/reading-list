
   <li class="row rating-<%= book.rating %>">
     <div class="col-sm-3">
       <a href="#" target="_blank">
         <%= image_from_amazon(book.amazon_id) %>
       </a>
       <p class="text-center rating"><strong><%= book.rating %></strong>/5</p>
     </div>

     <div class="col-sm-9">
       <h3><%= link_to book.title, '#' %></a></h3>
       <p class="text-muted"><em>Written by <%= book.author %></em></p>
       <div>
         <p>
           <%= book.description %>
         </p>
       </div>
       <span>
        <div class="col-sm-5">
           <p class"text-muted"> Genres: 
           <% book.genres.each do |genre| %>
             <%= link_to genre.name, books_path(filter: genre.name), class: 'label label-primary' %> 
           <% end %>
        </div>
        <div align="right">
          <%= link_to 'Delete book', book, method: :delete, data: { confirm: 'Are you sure?' }, class: 'label label-primary' %>
        </div> 
        </p>
       </span>
     </div>
   </li>
